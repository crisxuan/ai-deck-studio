# Planner Prompts

These prompts guide agents through the narrative-first workflow.

Use them as working documents, not as a hidden magic layer. The rendered deck should still be fully explainable from `deck.json`.
