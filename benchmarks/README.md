# Benchmarks

Fixed prompts for effect-driven deck regression.

Run:

```bash
npm run benchmark
```

The command reads `benchmarks/briefs/*.md`, creates generated decks in `benchmarks/output/`, renders HTML, runs visual verification, exports PNG slides, and writes a contact sheet for each deck.
